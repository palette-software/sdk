sudo apt-get install python-sphinx

# The package python-sphinxcontrib-httpdomain in Ubuntu 14.04 is old (1.1.8)
# and >1.1.9 is needed, so get a newer version from pypi:

sudo pip install sphinxcontrib_httpdomain==1.1.9

# To uninstall:
sudo pip uninstall sphinxcontrib_httpdomain
